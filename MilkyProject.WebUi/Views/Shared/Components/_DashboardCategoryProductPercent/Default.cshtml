@* @model Dictionary<string, double> *@
@* <div class="col-md-6">
    <div class="white_shd full margin_bottom_30">
        <div class="full graph_head">
            <div class="heading1 margin_0">
                <h2>Progress Bar</h2>
            </div>
        </div>
        <div class="full progress_bar_inner">
            <div class="row">
                <div class="col-md-12">
                    <div class="progress_bar">
                        <!-- Skill Bars -->
                        @foreach (var item in Model)
                        {
                            var categoryName = item.Key;
                            var percentage = item.Value; // Yüzdelik değeri alıyoruz

                            <div class="progress skill-bar mb-2">
                                <div class="progress-bar progress-bar-animated progress-bar-striped" role="progressbar" aria-valuenow="@percentage" aria-valuemin="0" aria-valuemax="100" style="width: @percentage%;">
                                    <!-- Yüzdelik değeri burada barın içinde görünecek -->
                                    <span class="progress-bar-label">@percentage%</span>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> *@


@model Dictionary<string, int>

<div class="col-md-6">
    <div class="white_shd full margin_bottom_30">
        <div class="full graph_head">
            <div class="heading1 margin_0">
                <h2>Kategorilerde ki Ürünlerin Yüzdelik Dağılımları</h2>
            </div>
        </div>
        <div class="full progress_bar_inner">
            <div class="row">
                <div class="col-md-12">
                    <div class="progress_bar">
                        <!-- Dinamik Skill Bars -->
                        @foreach (var item in Model)
                        {
                            var categoryName = item.Key;
                            var percentage = item.Value;
                            <span class="skill" style="width:73%;">@categoryName</span>    
                            @* <span class="skill" style="width:@percentage%;">@categoryName <span class="info_valume">@percentage%</span></span> *@
                            <div class="progress skill-bar">
                                <div class="progress-bar progress-bar-animated progress-bar-striped" role="progressbar" aria-valuenow="@percentage" aria-valuemin="0" aria-valuemax="100" style="width: @percentage%;">
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

